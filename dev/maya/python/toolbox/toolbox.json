{
"General": {
	"productUI": {
		"command": "from python.general import productUI\nreload(productUI)\nproductUI.launch()",
		"icon": "productUI"
	},
	"workspaceUI": {
		"command": "from python.general import workspaceUI\nreload(workspaceUI)\nworkspaceUI.launch()",
		"icon": "workspaceUI"
	},
	"renameUI": {
		"command": "from python.lib import renameUI\nreload(renameUI)\nrenameUI.launch()",
		"icon": "renameUI"
	}
},
"Model": {
	"modelUI": {
		"command": "from python.model import modelUI\nreload(modelUI)\nmodelUI.launch()",
		"icon": "modelUI"
	}},
"Groom": {
	"xgenUI": {
		"command": "from python.groom import groomToolsUI\nreload(groomToolsUI)\ngroomToolsUI.launch()",
		"icon": "groomToolsUI"
	}
},
"Lookdev": {
	"renderUI": {
		"command": "from python.lib import renderUI\nreload(renderUI)\nrenderUI.launch()",
		"icon": "renderUI"
	}
},
"Rig": {
	"rigUI": {
		"command": "from python.rig import rigUI\nreload(rigUI)\nrigUI.launch()",
		"icon": "rigUI"
	},
	"rigToolsUI": {
		"command": "from python.rig import rigToolsUI\nreload(rigToolsUI)\nrigToolsUI.launch()",
		"icon": "rigToolsUI"
	}
}
}